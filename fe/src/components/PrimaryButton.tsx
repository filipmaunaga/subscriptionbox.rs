import React from "react";
import "../styles/components/PrimaryButton.scss";

const PrimaryButton = () => {
  return (
    <button className="primary-button">
      <p className="primary-button-text">PrimaryButton</p>
    </button>
  );
};

export default PrimaryButton;
